import { Calendar } from 'react-big-calendar';

export default function Reservation() {
  return (
    <div>
      <div>{/* 드롭다운 필터 자리 */}</div>
      <div>{/* 달력 라이브러리 사용해야 할 자리 */}</div>
    </div>
  );
}
